# PDIV 报表生成工具 v1.0

把新的原始数据表丢进来，一次生成 Excel 报表和网页版趋势图。

## 怎么用

1. 双击 **`运行_PDIV报表工具.bat`**
2. 「源数据文件」→ 选择新的原始数据表
3. 「输出目录」→ 默认与源文件同目录，可以改
4. 勾选要生成的语言（中文 / English）和格式（Excel / 网页）
5. 点「开始生成」，完成后自动打开输出目录

文件名的版本号会自动递增（`_v1`、`_v2`…），**不会覆盖已有报表**。

## 对源数据表的要求

沿用现在这张表的布局，改动了会校验不通过：

| 位置 | 内容 |
|---|---|
| 第 3 行 | 表头 |
| 第 4 行起 | 数据 |
| A 列 | 日期，形如 `2026.09.02` |
| B~E 列 | A 站：生产数量、PDIV 1 次 NOK、2 次 NOK、3 次 NOK |
| F~I 列 | B 站：同上 |

NOK 列写成 `12/5.3%` 或 `0` 都能认，工具只取斜杠前面的数量，不合格率一律按
`NOK 数量 ÷ 当日生产数量` 重算，保证口径统一。

日期行数不限，多少天都行；空行自动跳过。

## 生成什么

| 文件 | 内容 |
|---|---|
| `PDIV检测数据统计_A站B站_vN.xlsx` | 6 张表、14 张曲线图。首页是「趋势图表」 |
| `PDIV检测数据趋势_vN.html` | 单文件离线网页，7 张交互曲线图，双击用浏览器打开 |
| `PDIV_Inspection_Statistics_A_vs_B_vN.xlsx` | 英文版 Excel |
| `PDIV_Reject_Rate_Trend_vN.html` | 英文版网页 |

## 首次使用需要装一个库

命令行里跑一次就行，以后不用再装：

```
pip install openpyxl
```

没装的话工具会弹窗提示这条命令。

## 不想用界面

命令行也能跑：

```
python pdiv_report_gui_v1.0.0.py 源数据.xlsx 输出目录 --zh --en
```

可选开关：`--zh` 中文、`--en` 英文（都不写默认中文）、`--no-xlsx`、`--no-html`。

## 文件夹里都是什么

| 文件 | 作用 |
|---|---|
| `运行_PDIV报表工具.bat` | 双击入口，自动挑版本号最高的界面脚本运行 |
| `pdiv_report_gui_v1.0.0.py` | 界面 + 调度 + 源文件校验 |
| `pdiv_nok_report_v1.2.1.py` | 生成 Excel 报表 |
| `pdiv_nok_html_report_v1.2.0.py` | 生成网页报表 |

以后哪个脚本要改，直接放新版本进来（文件名版本号更高即可），
`.bat` 和界面脚本都会自动认最新的那个，旧版留着不影响。

> 注意：`.bat` 按文件名倒序挑版本，所以 `v1.10` 会排在 `v1.9` 前面出问题。
> 真到了两位数版本，把旧版挪走或直接改 `.bat` 里的脚本名即可。
